There is no text 

this is the new line to see the diff

git add 

git commit -m 'this is the message new line added'

git diff 


git push


the git diff shows the new change to the file changes in the local scope

use the vs 